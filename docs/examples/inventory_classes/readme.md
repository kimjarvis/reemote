## Inventory classes example

This example demonstrates the construction of an inventory by compounding 
classes.

The inventory is constructed from InventoryItem objects, each of which represents a host.
The Connection object describes the ssh connection to the host.
The groups parameter lists the groups to which the host belongs.
