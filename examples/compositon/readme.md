## Composition example

This example demonstrates the composition of operations.

Operations are composed by creating a tree of classes. 
This example demonstrates the composition of operations.  
The shell command can be executed in a class tree. The execute method of the Root class yields
the result of the execute method of the child class.
